
<% provide(:title, 'Add a new Prop') %>

<h2>Create a new prop!</h2>

<%# if flash[:errors]%>
   <%# flash[:errors].each do |error| %>
        <!--<p style="color: red"><%#= error%></p> -->
    <%#end%>
<%#end%>

<%= form_for @prop do |f| %>
    <%= f.label :content%>
    <%= f.text_field :content %>
    <%= f.label :user_id %>
    <%= f.collection_select :user_id, User.all, :id, :name %>
    
    
    <%= f.submit %>
<% end %>